# Analysis / DMDc side (simulator-agnostic)
numpy>=1.24.0
matplotlib>=3.7.0

# Simulation side - install via your ROS 2 distro's package manager,
# NOT pip. See README.md "Prerequisites" for apt install commands.
#   ros-<distro>-ros-gz-sim
#   ros-<distro>-ros-gz-bridge
#   ros-<distro>-actuator-msgs
#   gz-sim (Harmonic) or matching Gazebo version for your ROS 2 distro
